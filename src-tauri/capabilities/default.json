{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permisos de la ventana de la mascota. La mayoría de acciones (mover, esconder, click-through) se hacen desde Rust y no necesitan permiso; el frontend solo escucha eventos.",
  "windows": ["mascota"],
  "permissions": [
    "core:default",
    "core:event:default"
  ]
}
